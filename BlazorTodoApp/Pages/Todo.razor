@page "/todo"

<PageTitle>Todo</PageTitle>

<h3>Todo (@todos.Count(todo => !todo.IsDone))</h3>

<ul>
    @foreach (var todo in todos)
    {
        <TodoItemComponent @bind-IsDone="todo.IsDone" @bind-Title="todo.Title" />
    }
</ul>

<input placeholder="Something todo" @bind="newTodoTitle" />
<button @onclick="AddTodo">Add todo</button>

@code {
    private List<TodoItem> todos = new() { new TodoItem { Title = "Todo 1", IsDone = true } };

    public int index;

    private string newTodoTitle = string.Empty;
    private void AddTodo()
    {
        todos.Add(new TodoItem { Title = newTodoTitle });
        newTodoTitle = string.Empty;
    }
}
